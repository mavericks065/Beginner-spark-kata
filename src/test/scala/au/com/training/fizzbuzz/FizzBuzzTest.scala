package au.com.training.fizzbuzz

import org.scalatest._

class FizzBuzzTest extends FlatSpec with Matchers {

  it should "return fizz if the number is dividable by 3" in {
    // GIVEN

    // WHEN

    // THEN
  }

  it should "return buzz if the number is dividable by 5" ignore {
    // GIVEN

    // WHEN

    // THEN
  }

  it should "return fizzbuzz if the number is dividable by 15" ignore {
    // GIVEN

    // WHEN

    // THEN
  }

  it should "return the number if the number is not dividable by 3/5/15" ignore {
    // GIVEN

    // WHEN

    // THEN
  }

}
